
# Git Constraints
- **NEVER** push code to remote repositories (e.g., git push) automatically unless explicitly asked by the user in that exact moment. The user requested 'dont push anything hereafter'.
